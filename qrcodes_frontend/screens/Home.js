import React from "react";
import {
    View,
    Text, SafeAreaView
}
    from "react-native"
import {COLORS, SIZES, FONTS, icons, images} from "../constants"

function Home() {
    return (
        <SafeAreaView>
            <Text>Home Page Where all recent QR-Codes should be displayed after successful autorisation!</Text>
        </SafeAreaView>
    )
}

export default Home;